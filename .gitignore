# Ambiente e dependências
.venv/
__pycache__/
*.pyc
*.pyo

# Dados locais (data lake bruto, banco) — nunca versionar
dados/

# Chaves de API — nunca versionar (use apis.example.yaml como modelo)
config/apis.yaml
.streamlit/secrets.toml

# Documentos internos de pesquisa (permanecem locais)
docs-pesquisa-implementacao/

# Sumários e prompts de sessão (memória operacional local, não publicar)
docs/sumarios-prompts-sessoes/

# Bateria de roteiros de teste (conteúdo local de testagem, não publicar)
testes/

# Sistema
Thumbs.db
.DS_Store
